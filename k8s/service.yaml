apiVersion: v1
kind: Service
metadata:
  name: portfolio-api
  namespace: portfolio
spec:
  type: NodePort
  selector:
    app: portfolio-api
  ports:
  - port: 3000
    targetPort: 3000
    nodePort: 30300